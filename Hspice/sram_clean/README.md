# SRAM clean-room simulation flow

This directory is the only active flow for the rebuilt SRAM experiment. Each gate has independent input, output, log, and validation status. A later gate is run only after the previous gate passes.

User test-plan prompt retained verbatim in `USER_SRAM_TEST_PLAN.md`.
