prompt_for gen_sde.py

现在文件夹内已经有很多生成SDE相关的文档，你需要总结前面的方法论（搜索在文件夹中前面提出的模型框架），同时现在需要对gen_sde.py进行颠覆性改进：
1）命令行执行 python gen_sde.py ./gds/xxxx.txt ./rules/xxx_arch.txt ./rules/xxx_layers.txt -o ./output/xxx.cmd后直接生成xxx.cmd，没有其他中间文件；
2）我需要你充分解耦生成sde文件需要的硬性参数和获取来源，删除重复定义的参数，避免参数泛化能力不强的问题（在堆叠结构中对某个层高度的设置会影响到上方结构的起始高度，容易和layer中设置的产生冲突，我认为可以通过运算的方式生成对应坐标，即在arch中定义某层高度为a，其上方某结构为b，在layer层中设定上方某层起始位置为$a+$b+0.1，进行生成前优先进行替换放置产生冲突）；
3）我需要现在一个sde生成完整的全部结构，而非仅在当前版图中提到的结构，增强应对不同版图的泛化能力，例如我在版图中加入符合规范的新层后，能够正确生成对应结构（除了已经有的复杂结构，我新加的结构应该只会完成一些通过版图高度和版图范围确定的立方体）
4）arch中和版图本身相关的参数全部删除（如果可以从版图唯一读出就删除，例如m0_track_0_y1由于已经在版图中体现，不要在arch中继续保留，还有potential_p1，m1_direction、sram_topology等都不要保留）保留栅极宽度、网格密度等相关参数；
5）除了2）中提到的增加关联运算外，需要增加可变参数，例如设定某一变量为@a@运算变量标为@<a+b+0.1>@；我目前还没有进行这部分内容的设置，你不要凭空增加可变量，只需要增加可变量的形式化保留和处理即可；
6）我让你生成的文件只有gen_sde.py，xxx_sde.cmd是运行gen_sde.py后生成的，请在确保一致性和正确性的前提下生成对应文件
7）生成全部对应金属的顶底面作为contact，并根据版图中txt所提到的内容进行命名；
8）你现在需要修改和依赖的文件为standard_arch.txt，standard_layer_rule.txt，standard_sram_v1_2.txt（我会根据其他几个1.3，1.6等的生成新的sde判断泛化能力）



二轮Prompt:
1）
